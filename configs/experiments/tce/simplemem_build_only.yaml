users:
  - 001_user_001

runtime:
  baseline: simplemem
  experiment_name: simplemem_build_only
  run_id: main
  resume: true

data:
  benchmark: outputs/gemini_3_flash_preview/{user_id}/task_packs.json
  app_logs_path: outputs/gemini_3_flash_preview/{user_id}/app_log_large.json

output:
  prediction_path: results/simplemem/results/{user_id}/prediction/{run_name}/build_only.json

llm:
  provider: openai
  model: gpt-5-mini

retriever:
  provider: openai
  model: text-embedding-3-large

baseline_params:
  snapshot_dir: results/simplemem/snapshots/{run_name}/{user_id}
  data_storage_path: results/simplemem/runtime_data/{run_name}/{user_id}
  build_only: true
  allow_destructive_rebuild: true
