# Legacy Generation Checklist

This directory contains legacy generation scripts.

## Verification
- Ensure that `run_hipporag_generation_legacy.py` can still run if needed for baseline comparison.
- Verify that these scripts point to the correct index paths in `../outputs/`.
