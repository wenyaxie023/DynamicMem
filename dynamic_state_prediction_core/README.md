# dynamic_state_prediction_core

Shared generation/evaluation utilities for dynamic state prediction:
- value prediction (`snapshot_state`)
- evidence prediction (`evidence` with `app_log_id` per state key)

```python
from dynamic_state_prediction_core import evaluate_checkpoints, normalize_predictions
```
